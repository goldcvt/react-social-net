const friendsReducer = (state, action) => {
    switch (action.type) {
        case "abracadabra":
            return state
        default:
            return state;
    }
}

export default friendsReducer;